import React from 'react'
import { observer, inject } from 'mobx-react';

const Analytics = inject('company')(observer(() => {
    return (
        <div>
            
        </div>
    )
}));

export default Analytics;